zerops:
  - setup: adminer
    build:
      base: php@8.0
      buildCommands:
        - echo "There is nothing to build. Only deploy the code."
      deploy: ['./public']
    run:
      documentRoot: public
