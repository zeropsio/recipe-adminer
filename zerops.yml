zerops:
  - setup: adminer
    build:
      os: alpine
      base: php@latest
      buildCommands:
        - echo "There is nothing to build. Only deploy the code."
      deploy: ['./public']
    run:
      os: alpine
      base: php-apache@8.3
      documentRoot: public
