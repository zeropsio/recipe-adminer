zerops:
  - setup: adminer
    build:
      os: alpine
      base: php@8.3
      buildCommands:
        - echo "There is nothing to build. Only deploy the code."
      deployFiles: ['./public']
    run:
      os: alpine
      base: php-apache@8.3
      documentRoot: public
